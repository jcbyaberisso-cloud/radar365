import {NextResponse} from 'next/server';import{oddsPapiAccount}from '../../../../lib/oddspapi';export const dynamic='force-dynamic';export async function GET(){try{const a:any=await oddsPapiAccount();const text=JSON.stringify(a);const props=/player.?props/i.test(text);return NextResponse.json({ok:true,configured:true,playerPropsMentioned:props,account:a})}catch(e){return NextResponse.json({ok:false,configured:Boolean(process.env.ODDSPAPI_API_KEY),error:e instanceof Error?e.message:'OddsPapi check failed'},{status:500})}}
