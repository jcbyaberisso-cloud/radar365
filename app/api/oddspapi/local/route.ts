import{NextResponse}from'next/server';import{bet365ArgentinaFixtures,bet365ArgentinaOdds,normalizeBet365ArgentinaOdds}from'../../../../lib/oddspapi';export const dynamic='force-dynamic';export const maxDuration=60;
export async function GET(){try{const now=new Date(),to=new Date(now.getTime()+47*3600000);const fixtures:any=await bet365ArgentinaFixtures(now.toISOString(),to.toISOString());const xs=Array.isArray(fixtures)?fixtures:(fixtures?.fixtures??fixtures?.data??[]);let chosen:any=null,normalized:any[]=[];for(const f of xs.slice(0,4)){if(!f?.fixtureId)continue;const raw:any=await bet365ArgentinaOdds({fixtureId:String(f.fixtureId)});const n=normalizeBet365ArgentinaOdds(raw);if(n.length){chosen=f;normalized=n;break}}const playerProps=normalized.filter(o=>Boolean(o.player));return NextResponse.json({ok:true,bookmaker:'bet365.bet.ar',fixtures:xs.length,fixture:chosen?{fixtureId:chosen.fixtureId,home:chosen.participant1Name,away:chosen.participant2Name,startTime:chosen.startTime}:null,normalizedMarkets:normalized.length,playerProps:playerProps.length,sample:playerProps.slice(0,8).map(o=>({market:o.marketName,player:o.player,outcome:o.outcome,line:o.line,odds:o.decimalOdds}))})}catch(e){return NextResponse.json({ok:false,error:e instanceof Error?e.message:'OddsPapi local feed failed'},{status:500})}}
